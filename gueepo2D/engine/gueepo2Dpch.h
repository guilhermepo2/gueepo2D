#pragma once

#if PLATFORM_WINDOWS
// #todo: include windows stuff (probably will have to do a bunch of platform checking here)
#endif

#include <string>
#include <fstream>
#include <sstream>
#include <functional>